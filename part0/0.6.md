sequenceDiagram
    participant browser
    participant server

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    server-->>browser: 201 status code
    deactivate server

    Note right of browser: The browser executes the JavaScript code fetched from the server.
    The code creates a new note, adds it to the notes lits with the command notes.push(note),
    refresh the note list on the page and sends the new note to the server.

    
    
