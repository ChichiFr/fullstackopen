```mermaid
sequenceDiagram
    participant browser
    participant server

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    server-->>browser: 201 status code
    deactivate server

    Note right of browser: The browser executes the JavaScript code fetched from the server. The code creates a new note and refresh the note litst from the browser.

    
    
